<?php

define('DIR_APP', str_replace(['\\', '/'], DS, DIR_ROOT . 'app' . DS));

define('DIR_BOOTSTRAP', str_replace(['\\', '/'], DS, DIR_ROOT . 'bootstrap' . DS));

define('DIR_NOVA', str_replace(['\\', '/'], DS, DIR_ROOT . 'lib' . DS));

define('DIR_STORAGE', str_replace(['\\', '/'], DS, DIR_ROOT . 'storage' . DS));

define('DIR_CONFIG', str_replace(['\\', '/'], DS, DIR_ROOT . 'config' . DS));

define('DIR_BUILD', str_replace(['\\', '/'], DS, DIR_ROOT . 'build' . DS));
